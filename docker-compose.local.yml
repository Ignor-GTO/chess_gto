# Локальный запуск без Dokploy Traefik (http://localhost:8080)
# docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build

services:
  nginx:
    ports:
      - "8080:80"

networks:
  dokploy-network:
    external: false
